version: '3.8'

services:
  db:
    container_name: lab4_db
    image: postgres:13
    environment:
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
      POSTGRES_DB: mydb
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U myuser -d mydb"]
      interval: 5s
      timeout: 5s
      retries: 5

  vault:
    container_name: lab4_vault
    image: hashicorp/vault:latest
    ports:
      - "8200:8200"
    environment:
      VAULT_DEV_ROOT_TOKEN_ID: "myroot"
      VAULT_DEV_LISTEN_ADDRESS: "0.0.0.0:8200"

  vault-init:
    container_name: lab4_vault_init
    image: hashicorp/vault:latest
    depends_on:
      - vault
    environment:
      VAULT_ADDR: "http://lab4_vault:8200"
      VAULT_TOKEN: "myroot"
    command:
      - "sh"
      - "-c"
      - |
        sleep 5
        vault secrets enable -path=secret kv-v2
        vault kv put secret/db_credentials username=myuser password=mypassword
        echo "Секреты успешно загружены в Vault!"

  zookeeper:
    image: confluentinc/cp-zookeeper:7.4.0
    container_name: lab4_zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000

  kafka:
    image: confluentinc/cp-kafka:7.4.0
    container_name: lab4_kafka
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1

  model_app:
    container_name: lab4_model_app
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - kafka
    environment:
      KAFKA_BROKER: "lab4_kafka:29092"

  consumer_app:
    container_name: lab4_consumer_app
    build: .
    command: ["python", "consumer.py"]
    depends_on:
      vault-init:
        condition: service_completed_successfully
      db:
        condition: service_healthy
      kafka:
        condition: service_started
    environment:
      VAULT_ADDR: "http://lab4_vault:8200"
      VAULT_TOKEN: "myroot"
      DB_HOST: "lab4_db"
      POSTGRES_DB: "mydb"
      KAFKA_BROKER: "lab4_kafka:29092"